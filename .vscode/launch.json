{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Build and Run Clicker Game",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/clickGame.exe",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [
                {
                    "name": "PATH",
                    "value": "C:/msys64/ucrt64/bin;${env:PATH}"
                }
            ],
            "externalConsole": false,
            "MIMode": "gdb",
            "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
            "preLaunchTask": "CMake Build Clicker Game"
        }
    ]
}